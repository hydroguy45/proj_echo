package Map;

import java.awt.Color;

import javax.swing.BorderFactory;
import javax.swing.JLabel;
import javax.swing.JPanel;

@SuppressWarnings("serial")
public class Elements extends JPanel {
	public Elements(){
		setBorder(BorderFactory.createTitledBorder(BorderFactory.createLineBorder(Color.BLACK),"Map Elements"));
		JLabel tempText = new JLabel("Add Frame about Platforms and another frame about pickups", JLabel.CENTER);
		add(tempText);
	}
}
